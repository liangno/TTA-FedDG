data_root: "/home/lianghy68/data/PACS/raw_images/Train val splits and h5py files pre-read"
# data_root: "data/DomainNet/"
# dataset: "domainnet-126"

# train_ratio: 0.9            ## Ratio of training data used in source training
# source_domains:
#   - "real"
# target_domains:
#   - "sketch"
# pl_list: null               ## Cached initial pseudo labels to save time
# aug_type: "moco-v2"         ## Used in TTA contrastive learning

# batch_size: 128             ## Batch size should be expressed as ""2^n"". Other wise it freezes. For DomainNet, USe large batch size. 
# workers: 8

dataset: "photo"

target_domains:
  - "sketch"
fed_domains:
  - "pac"
pl_list: null               ## Cached initial pseudo labels to save time
aug_type: "moco"         ## Used in TTA contrastive learning

batch_size: 32
           ## Batch size should be expressed as ""2^n"". Other wise it freezes. For DomainNet, USe large batch size. 
workers: 8